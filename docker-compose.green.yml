version: '3'
services:
  green-app:
    build:
      context: ./server
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    networks:
      - default

  frontend:
    container_name: green-frontend
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "8082:80"
